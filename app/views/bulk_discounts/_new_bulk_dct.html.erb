<%= form_with url: path, method: method, data: {turbo: false}, local: true do |f| %>

  <%= f.label :percentage_discount %>
  <%= f.text_field :percentage_discount, value: value_percentage || '' %><br>

  <%= f.label :quantity_treshold %>
  <%= f.text_field :quantity_treshold, value: value_quantity || '' %>

  <%= f.submit button_text %>
<% end %>